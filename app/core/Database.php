<?php

class Database {
    private $host = DB_HOST,
            $user = DB_USER,
            $pass = DB_PASS,
            $name = DB_NAME,
            $dbh,
            $stmt;

    public function __construct()
    {
        
    }

}